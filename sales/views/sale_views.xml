<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Sales List View -->
        <record id="view_havanoposdesk_sale_tree" model="ir.ui.view">
            <field name="name">havanoposdesk.sale.tree</field>
            <field name="model">havanoposdesk.sale</field>
            <field name="arch" type="xml">
                <list string="Sales Invoices" default_order="date desc">
                    <field name="name"/>
                    <field name="is_return" optional="show" readonly="1"/>
                    <field name="payment_status" optional="show"/>
                    <field name="customer"/>
                    <field name="date"/>
                    <field name="store_id"/>
                    <field name="salesperson_id"/>
                    <field name="amount_untaxed" string="Total Exclu" sum="Total Exclu"/>
                    <field name="amount_tax" string="Total Tax" sum="Total Tax"/>
                    <field name="amount_total" string="Total Inclu" sum="Total Inclu"/>
                    <field name="state" widget="badge" decoration-info="state == 'draft'" decoration-warning="state == 'confirmed'" decoration-success="state == 'done'"/>
                </list>
            </field>
        </record>

        <!-- Sales Form View -->
        <record id="view_havanoposdesk_sale_form" model="ir.ui.view">
            <field name="name">havanoposdesk.sale.form</field>
            <field name="model">havanoposdesk.sale</field>
            <field name="arch" type="xml">
                <form string="Sales Invoice">
                    <header>
                        <button name="%(action_havanoposdesk_sale_return_wizard)d" string="Credit Note" type="action" class="btn-primary" invisible="state != 'done' or is_return"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,done"/>
                    </header>
                    <sheet>
                        <div class="oe_title">
                            <h1>
                                <field name="name" readonly="1"/>
                            </h1>
                            <h2>
                                <span invisible="not is_return" class="text-danger">Credit Note</span>
                            </h2>
                        </div>
                        <group>
                            <group>
                                <field name="customer" class="border-bottom border-dark"/>
                                <field name="store_id" domain="[('tenant_id', '=', tenant_id)]" options="{'no_create': True}" class="border-bottom border-dark"/>
                                <field name="date" class="border-bottom border-dark"/>
                                <field name="is_return" invisible="1"/>
                                <field name="return_id" invisible="not is_return" readonly="1" class="border-bottom border-dark"/>
                            </group>
                            <group>
                                <field name="salesperson_id" options="{'no_create': True}" class="border-bottom border-dark"/>
                                <field name="payment_status" class="border-bottom border-dark" widget="radio" options="{'horizontal': true}"/>
                                <field name="account_id" invisible="payment_status != 'cash'" required="payment_status == 'cash'" class="border-bottom border-dark"/>
                                <field name="tenant_id" invisible="1"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Order Lines">
                                <field name="line_ids">
                                    <list editable="bottom">
                                        <field name="product_id" domain="[('store_id', '=', parent.store_id)]" width="25%" optional="show"/>
                                        <field name="item_code" width="10%" optional="show"/>
                                        <field name="accepted_qty" width="15%" sum="Total Qty" optional="show"/>
                                        <field name="rate" width="15%" optional="show"/>
                                        <field name="tax_ids" widget="many2many_tags" width="15%" optional="show"/>
                                        <field name="price_tax" string="Tax Amount" width="10%" sum="Total Tax" optional="show"/>
                                        <field name="amount" width="10%" sum="Total Amount" optional="show"/>
                                    </list>
                                </field>
                                <group class="oe_subtotal_footer oe_right" colspan="2" name="sale_total">
                                    <field name="amount_untaxed" widget="monetary" string="Total Excl"/>
                                    <field name="amount_tax" widget="monetary" string="Total Tax"/>
                                    <field name="amount_total" class="oe_subtotal_footer_separator" widget="monetary" string="Total Incl"/>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Sales Graph View -->
        <record id="view_havanoposdesk_sale_graph" model="ir.ui.view">
            <field name="name">havanoposdesk.sale.graph</field>
            <field name="model">havanoposdesk.sale</field>
            <field name="arch" type="xml">
                <graph string="Sales Analysis" type="bar" sample="1">
                    <field name="date" interval="day"/>
                    <field name="amount_total" type="measure"/>
                </graph>
            </field>
        </record>

        <!-- Sales Pivot View -->
        <record id="view_havanoposdesk_sale_pivot" model="ir.ui.view">
            <field name="name">havanoposdesk.sale.pivot</field>
            <field name="model">havanoposdesk.sale</field>
            <field name="arch" type="xml">
                <pivot string="Sales Analysis" sample="1">
                    <field name="date" interval="month" type="row"/>
                    <field name="store_id" type="col"/>
                    <field name="amount_total" type="measure"/>
                </pivot>
            </field>
        </record>

        <!-- Action: Sales Operations -->
        <record id="action_sales_operations" model="ir.actions.act_window">
            <field name="name">Sales Invoices</field>
            <field name="res_model">havanoposdesk.sale</field>
            <field name="view_mode">list,form</field>
            <field name="domain">[]</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Record your first sale!
                </p>
            </field>
        </record>

        <!-- Action: Sales Analytics -->
        <record id="action_sales_analysis" model="ir.actions.act_window">
            <field name="name">Sales Analytics</field>
            <field name="res_model">havanoposdesk.sale</field>
            <field name="view_mode">graph,pivot</field>
            <field name="domain">[]</field>
        </record>

        <!-- Sales App Menu Connections -->
        <menuitem id="menu_sales_orders" name="Sales Invoices" parent="menu_sales_operations" action="action_sales_operations" sequence="10"/>
        <menuitem id="menu_sales_analysis" name="Sales Analysis" parent="menu_sales_reports" action="action_sales_analysis" sequence="10"/>
    </data>
</odoo>
