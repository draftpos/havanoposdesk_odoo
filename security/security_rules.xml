<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Custom group for Tenant Admins -->
        <!-- implied_ids includes group_erp_manager so the Settings icon appears on the home screen -->
        <record id="group_tenant_admin" model="res.groups">
            <field name="name">Tenant Admin</field>
            <field name="implied_ids" eval="[(4, ref('base.group_user')), (4, ref('base.group_erp_manager'))]"/>
        </record>

        <!-- Tenant Model Isolation Rule -->
        <record id="rule_havano_tenant_isolation" model="ir.rule">
            <field name="name">Havano Tenant Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_tenant"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Category Model Isolation Rule -->
        <record id="rule_havano_category_isolation" model="ir.rule">
            <field name="name">Havano Category Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_category"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids)] if (user.havano_role == 'admin' and user.store_ids) or user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- UOM Model Isolation Rule -->
        <record id="rule_havano_uom_isolation" model="ir.rule">
            <field name="name">Havano UOM Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_uom"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Pricelist Model Isolation Rule -->
        <record id="rule_havano_pricelist_isolation" model="ir.rule">
            <field name="name">Havano Pricelist Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_pricelist"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Product UOM Price Model Isolation Rule -->
        <record id="rule_havano_product_uom_price_isolation" model="ir.rule">
            <field name="name">Havano Product UOM Price Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_product_uom_price"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Store Model Isolation Rule -->
        <record id="rule_havano_store_isolation" model="ir.rule">
            <field name="name">Havano Store Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_store"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('id', 'in', user.store_ids.ids)] if (user.havano_role == 'admin' and user.store_ids) or user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Product Model Isolation Rule -->
        <record id="rule_havano_product_isolation" model="ir.rule">
            <field name="name">Havano Product Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_product"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids)] if (user.havano_role == 'admin' and user.store_ids) or user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Sale Model Isolation Rule -->
        <record id="rule_havano_sale_isolation" model="ir.rule">
            <field name="name">Havano Sale Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_sale"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids)] if (user.havano_role == 'admin' and user.store_ids) or user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Supplier Model Isolation Rule -->
        <record id="rule_havano_supplier_isolation" model="ir.rule">
            <field name="name">Havano Supplier Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_supplier"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids)] if (user.havano_role == 'admin' and user.store_ids) or user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- POS Terminal Model Isolation Rule -->
        <record id="rule_havano_pos_terminal_isolation" model="ir.rule">
            <field name="name">Havano POS Terminal Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_pos_terminal"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids)] if (user.havano_role == 'admin' and user.store_ids) or user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Subscription Plan Model Isolation Rule (Allow Read for All Users) -->
        <record id="rule_havano_subscription_plan_isolation" model="ir.rule">
            <field name="name">Havano Subscription Plan Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_subscription_plan"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[]</field>
        </record>

        <!-- User Model Isolation Rule -->
        <record id="rule_havano_user_isolation" model="ir.rule">
            <field name="name">Havano User Isolation</field>
            <field name="model_id" ref="base.model_res_users"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Account Model Isolation Rule -->
        <record id="rule_havano_account_isolation" model="ir.rule">
            <field name="name">Havano Account Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_account"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Payment Model Isolation Rule -->
        <record id="rule_havano_payment_isolation" model="ir.rule">
            <field name="name">Havano Payment Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_payment"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Stock Valuation Model Isolation Rule -->
        <record id="rule_havano_stock_valuation_isolation" model="ir.rule">
            <field name="name">Havano Stock Valuation Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_stock_valuation"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('product_id.store_id', 'in', user.store_ids.ids)] if (user.havano_role == 'admin' and user.store_ids) or user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Stock Ledger Model Isolation Rule -->
        <record id="rule_havano_stock_ledger_isolation" model="ir.rule">
            <field name="name">Havano Stock Ledger Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_stock_ledger"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('product_id.store_id', 'in', user.store_ids.ids)] if (user.havano_role == 'admin' and user.store_ids) or user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Customer Model Isolation Rule -->
        <record id="rule_havano_customer_isolation" model="ir.rule">
            <field name="name">Havano Customer Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_customer"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids)] if (user.havano_role == 'admin' and user.store_ids) or user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Tax Model Isolation Rule -->
        <record id="rule_havano_tax_isolation" model="ir.rule">
            <field name="name">Havano Tax Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_tax"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[]</field>
        </record>

        <!-- Purchase Model Isolation Rule -->
        <record id="rule_havano_purchase_isolation" model="ir.rule">
            <field name="name">Havano Purchase Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_purchase"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids)] if (user.havano_role == 'admin' and user.store_ids) or user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Stock Adjustment Model Isolation Rule -->
        <record id="rule_havano_stock_adjustment_isolation" model="ir.rule">
            <field name="name">Havano Stock Adjustment Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_stock_adjustment"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids)] if (user.havano_role == 'admin' and user.store_ids) or user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Sale Line Model Isolation Rule -->
        <record id="rule_havano_sale_line_isolation" model="ir.rule">
            <field name="name">Havano Sale Line Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_sale_line"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Purchase Line Model Isolation Rule -->
        <record id="rule_havano_purchase_line_isolation" model="ir.rule">
            <field name="name">Havano Purchase Line Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_purchase_line"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- User Rights Profile Model Isolation Rule -->
        <record id="rule_havano_user_rights_profile_isolation" model="ir.rule">
            <field name="name">Havano User Rights Profile Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_user_rights_profile"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- User Rights Permission Model Isolation Rule -->
        <record id="rule_havano_user_rights_permission_isolation" model="ir.rule">
            <field name="name">Havano User Rights Permission Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_user_rights_permission"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('profile_id.tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Item Profitability Report Isolation Rule -->
        <record id="rule_havano_item_profitability_report_isolation" model="ir.rule">
            <field name="name">Havano Item Profitability Report Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_item_profitability_report"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids)] if (user.havano_role == 'admin' and user.store_ids) or user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Category Sales Report Model Isolation Rule -->
        <record id="rule_havano_category_sales_report_isolation" model="ir.rule">
            <field name="name">Havano Category Sales Report Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_category_sales_report"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids)] if (user.havano_role == 'admin' and user.store_ids) or user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Cashier Sales Report Model Isolation Rule -->
        <record id="rule_havano_cashier_sales_report_isolation" model="ir.rule">
            <field name="name">Havano Cashier Sales Report Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_cashier_sales_report"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids), ('salesperson_id', '=', user.id)] if user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids)] if user.havano_role == 'admin' and user.store_ids else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Terminal Sales Report Model Isolation Rule -->
        <record id="rule_havano_terminal_sales_report_isolation" model="ir.rule">
            <field name="name">Havano Terminal Sales Report Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_terminal_sales_report"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids), ('salesperson_id', '=', user.id)] if user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids)] if user.havano_role == 'admin' and user.store_ids else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>

        <!-- Daily Sales Report Model Isolation Rule -->
        <record id="rule_havano_daily_sales_report_isolation" model="ir.rule">
            <field name="name">Havano Daily Sales Report Isolation</field>
            <field name="model_id" ref="model_havanoposdesk_daily_sales_report"/>
            <field name="global" eval="True"/>
            <field name="domain_force">[] if user.havano_role == 'super_admin' or not user.tenant_id else [('tenant_id', '=', user.tenant_id.id), ('store_id', 'in', user.store_ids.ids)] if (user.havano_role == 'admin' and user.store_ids) or user.havano_role == 'user' else [('tenant_id', '=', user.tenant_id.id)]</field>
        </record>
    </data>
</odoo>
