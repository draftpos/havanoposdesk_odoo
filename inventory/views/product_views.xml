<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Product List (Tree) View -->
        <record id="view_havanoposdesk_product_tree" model="ir.ui.view">
            <field name="name">havanoposdesk.product.tree</field>
            <field name="model">havanoposdesk.product</field>
            <field name="arch" type="xml">
                <list string="Products">
                    <field name="name"/>
                    <field name="item_code" optional="show"/>
                    <field name="category_id" optional="show"/>
                    <field name="uom_id" optional="show"/>
                    <field name="buying_price" optional="show"/>
                    <field name="selling_price" optional="show"/>
                    <field name="track_qty" optional="show"/>
                    <field name="opening_stock" string="Stock on Hand" optional="show"/>
                    <field name="color_hex" widget="color" optional="hide"/>
                    <field name="tenant_id" column_invisible="1"/>
                </list>
            </field>
        </record>

        <!-- Product Form View -->
        <record id="view_havanoposdesk_product_form" model="ir.ui.view">
            <field name="name">havanoposdesk.product.form</field>
            <field name="model">havanoposdesk.product</field>
            <field name="arch" type="xml">
                <form string="Product">
                    <sheet>
                        <field name="image_1920" widget="image" class="oe_avatar" options="{'preview_image': 'image_1920'}"/>
                        
                        <div class="w-100 d-flex justify-content-center mb-3">
                            <notebook>
                                <page string="DETAILS">
                                    <group>
                                        <group string="Product information">
                                            <field name="item_code"/>
                                            <field name="category_id"/>
                                            <field name="uom_id"/>
                                            <field name="tenant_id" invisible="1"/>
                                        </group>
                                        <group string="Price information">
                                            <field name="buying_price"/>
                                            <field name="selling_price"/>
                                            <field name="markup"/>
                                            <field name="id" invisible="1"/>
                                            <field name="opening_stock" string="Opening Stock" invisible="id"/>
                                            <field name="opening_stock" string="Stock on Hand" invisible="not id" readonly="1"/>
                                        </group>
                                        <group string="Stock &amp; Appearance">
                                            <field name="track_qty"/>
                                            <field name="color_hex" widget="color" placeholder="e.g. ffffff"/>
                                        </group>
                                    </group>
                                </page>
                                <page string="ADVANCED PRICING">
                                    <group>
                                        <group>
                                            <field name="discount_percentage"/>
                                            <field name="tax_percentage"/>
                                        </group>
                                    </group>
                                </page>
                                <page string="OTHER">
                                    <group>
                                        <group>
                                            <field name="is_active"/>
                                            <field name="internal_notes"/>
                                        </group>
                                    </group>
                                </page>
                            </notebook>
                        </div>

                        <div class="oe_title text-center w-100 mt-3">
                            <label for="name" class="oe_edit_only" string="Product Name"/>
                            <h1>
                                <field name="name" placeholder="e.g. Delta Can 550ml"/>
                            </h1>
                        </div>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Product Search View -->
        <record id="view_havanoposdesk_product_search" model="ir.ui.view">
            <field name="name">havanoposdesk.product.search</field>
            <field name="model">havanoposdesk.product</field>
            <field name="arch" type="xml">
                <search string="Search Products">
                    <field name="name"/>
                    <field name="item_code"/>
                    <field name="category_id"/>
                    <field name="opening_stock" string="Stock on Hand"/>
                    <separator/>
                    <filter string="In Stock" name="in_stock" domain="[('opening_stock', '&gt;', 0)]"/>
                    <filter string="Out of Stock" name="out_of_stock" domain="[('opening_stock', '&lt;=', 0)]"/>
                </search>
            </field>
        </record>

        <!-- Action -->
        <record id="action_havanoposdesk_product" model="ir.actions.act_window">
            <field name="name">Products</field>
            <field name="res_model">havanoposdesk.product</field>
            <field name="view_mode">list,form</field>
            <field name="search_view_id" ref="view_havanoposdesk_product_search"/>
        </record>

        <!-- Menu Item -->
        <menuitem id="menu_product" name="Products" parent="menu_inventory_root" action="action_havanoposdesk_product" sequence="10"/>
    </data>
</odoo>
