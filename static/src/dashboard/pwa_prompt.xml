<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="havanoposdesk.PwaPrompt" owl="1">
        <div t-if="state.showPrompt" class="pwa-prompt-container position-fixed bottom-0 start-0 w-100 p-3 z-index-toast" style="z-index: 1050;">
            <div class="card shadow-lg border-0 rounded-4 bg-white overflow-hidden">
                <div class="card-body p-3 d-flex align-items-center justify-content-between">
                    <div class="d-flex align-items-center">
                        <div class="bg-primary bg-opacity-10 rounded-3 p-2 me-3 text-primary d-flex align-items-center justify-content-center" style="width: 48px; height: 48px;">
                            <i class="fa fa-download fa-lg"></i>
                        </div>
                        <div>
                            <h6 class="mb-1 fw-bold text-dark">Install App</h6>
                            <p t-if="!state.isIosSafari" class="mb-0 text-muted small">Add to your home screen for faster access and better experience.</p>
                            <p t-if="state.isIosSafari" class="mb-0 text-muted small">Tap the Share button <i class="fa fa-share-square-o mx-1"></i> and select <strong>"Add to Home Screen"</strong>.</p>
                        </div>
                    </div>
                    <div class="d-flex align-items-center gap-2">
                        <button t-if="!state.isIosSafari" class="btn btn-primary btn-sm rounded-pill px-3 fw-bold shadow-sm" t-on-click="installApp">Install</button>
                        <button class="btn btn-light btn-sm rounded-circle shadow-sm d-flex align-items-center justify-content-center text-muted" style="width: 32px; height: 32px; padding: 0;" t-on-click="dismiss">
                            <i class="fa fa-times"></i>
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </t>

</templates>
